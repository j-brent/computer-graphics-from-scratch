set(CGFS_BUILDING_WITH_EMSCRIPTEN FALSE)
set(TCFILE "fts")
if(DEFINED CMAKE_TOOLCHAIN_FILE)
  cmake_path(GET CMAKE_TOOLCHAIN_FILE FILENAME TCFILE)
endif()
if ("${TCFILE}" STREQUAL "Emscripten.cmake")
  set(CGFS_BUILDING_WITH_EMSCRIPTEN TRUE)
endif()

message("toolchain file: ${TCFILE}")
message("CGFS_BUILDING_WITH_EMSCRIPTEN: ${CGFS_BUILDING_WITH_EMSCRIPTEN}")
unset(TCFILE)
